# Weights

Place baseline weights here.

Expected file for the TMM experiments:

```text
efficient_sam_vitt.pt
```

Checkpoints produced by training are written to `outputs_sam_sirst_hq/` by
default and are intentionally ignored by git.

